# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Environment
.env
.env.local
.env.*.local

# Build outputs
dist/
build/
.next/
out/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Claude Code
.claude/

# Wrangler cache
.wrangler/

# Local docs
BUILD.md

# Legacy Python backend (kept locally for reference, never commit)
clearai-backend-python/

# MS Office lock files
~$*

# Shared client data — large binary files (xlsx/xml/pdf), kept out of
# repo to avoid bloat and to honour the data-sharing agreement that
# limits redistribution. Lives locally only.
naqel-shared-data/

# Internal coordination notes — agent handover briefs and ADRs that
# reference deployment internals, oids, and operational context. Keep
# local; do not publish to origin.
tracker/

# Local accuracy-test scaffolding (benchmarking scripts + fixtures).
# Not part of the runtime; not for the public repo.
scripts/accuracy-test/

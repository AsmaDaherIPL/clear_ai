// Owner: hs-classification agent.
// Existing expand logic still lives inline in expand.routes.ts; future split:
// extract use-case + controller + validation here. Done as part of HS-class agent
// follow-up after dispatch flow is finalized.

export {};

// Owner: BatchPlumber agent.
// Cached read of tenant_lookups (cities, currencies, countries, ports, etc).
// Expected exports: getLookups(tenantId, lookupType?) -> Map<sourceValue, canonicalValue>.

export {};

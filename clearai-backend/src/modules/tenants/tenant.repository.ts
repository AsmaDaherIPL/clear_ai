// Owner: BatchPlumber agent.
// Drizzle queries against the new tenants + tenant_field_mappings tables.
// Expected exports: getTenantBySlug, getTenantById, getMappings(tenantId), upsertTenant.

export {};

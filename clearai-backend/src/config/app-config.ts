// App-level constants and feature flags read from env (via env.ts) and frozen
// at startup. Goes here when a value is shared across multiple modules and
// you want a single typed import path.

export {};
